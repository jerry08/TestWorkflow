name: discord message
on: [push]
jobs:

  build:
    env:
      DISCORD_WEBHOOK: ${{ secrets.DISCORD_WEBHOOK }}
        
    name: Build
    runs-on: ubuntu-latest
    
    steps:
    - name: Discord notification
      uses: Ilshidur/action-discord@master
      with:
        args: 'The project test has been deployed.'
